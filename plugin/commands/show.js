const { 
    sendText,
} = require('../function/send')
module.exports = {
    name: 'show',
    usage: 'send /show',
    category: 'Utility',
    description: 'Show keyboard button markup.',
    execute(ctx, message, args, bot) {
        
    }
}